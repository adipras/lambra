# Environment variables for {{.ServiceName}}
PORT={{.Port}}
ENV={{.Environment}}
GIN_MODE={{.GinMode}}

# Database Configuration
DB_DRIVERNAME=mysql
DB_HOST={{.ServiceName}}-db
DB_PORT=3306
DB_USERNAME={{.DatabaseUser}}
DB_PASSWORD={{.DatabasePassword}}
DB_NAME={{.DatabaseName}}

# RBAC Service (if needed)
RBAC_SERVICE_URL={{.RBACServiceURL}}

# Ambassador Service (if needed)
AMBASSADOR_SERVICE_URL={{.AmbassadorServiceURL}}
